{
  "name": "my-web-q-xs",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
  "dev": "astro dev",
  "start": "astro dev",
  "build": "astro build",
  "preview": "astro preview",
  "deploy": "astro build && npx wrangler pages deploy dist"
},
  "dependencies": {
    "@astrojs/cloudflare": "^13.1.2",
    "@astrojs/mdx": "^5.0.1",
    "astro": "^6.0.5"
  }
}
